<UserControl x:Class="ControleFrota.Views.ListagemVeículosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ControleFrota.Views"
             xmlns:viewModels="clr-namespace:ControleFrota.ViewModels"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance viewModels:ListagemVeículosViewModel}"
             d:DesignHeight="1080" d:DesignWidth="1920" Language="pt-BR" IsHitTestVisible="{Binding HitTestVisible}">
    <UserControl.Resources>
        <ContextMenu x:Key="ContextVeículos">
            <MenuItem Header="Saída" Command="{Binding SaídaDeViatura}"/>
            <MenuItem Header="Retorno" Command="{Binding RetornoDeViatura}"/>
            <MenuItem Header="Abastecer" Command="{Binding AbastecerViatura}"/>
        </ContextMenu>
        <Style x:Key="RowStyle" TargetType="{x:Type DataGridRow}">
            <Setter Property="ContextMenu" Value="{StaticResource ContextVeículos}"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <local:MenuPadrãoView/>
        <Grid Grid.Row="1" Margin="5">
            <DataGrid ItemsSource="{Binding Veículos}" SelectedItem="{Binding VeículoSelecionado, UpdateSourceTrigger=PropertyChanged, Mode=OneWayToSource}" AutoGenerateColumns="False"
                      CanUserAddRows="False" HeadersVisibility="Column" CanUserReorderColumns="False" IsReadOnly="True">
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Saída" Command="{Binding SaídaDeViatura}"/>
                        <MenuItem Header="Retorno" Command="{Binding RetornoDeViatura}"/>
                        <MenuItem Header="Abastecer" Command="{Binding AbastecerViatura}"/>
                    </ContextMenu>
                </DataGrid.ContextMenu>
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="FontSize" Value="18"/>
                        <Setter Property="FontWeight" Value="DemiBold"/>
                        <Setter Property="Padding" Value="15,5"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Placa" Binding="{Binding Placa}" FontSize="17" />
                    <DataGridTextColumn Header="Marca" Binding="{Binding Marca.Nome}" FontSize="17"/>
                    <DataGridTextColumn Header="Modelo" Binding="{Binding Modelo.Nome}" FontSize="17"/>
                    <DataGridTextColumn Header="Quilometragem" Binding="{Binding ÚltimaQuilometragemInformada, StringFormat={}{0:N0} km}" FontSize="17"/>
                    <DataGridTextColumn Header="RENAVAM" Binding="{Binding RENAVAM}" FontSize="17"/>
                    <DataGridTextColumn Header="Último Licenciamento" Binding="{Binding ÚltimoLicenciamento, StringFormat=dd MMM yyyy}" FontSize="17"/>
                    <DataGridTextColumn Header="Último Abastecimento" Binding="{Binding ÚltimoAbastecimento}" FontSize="17"/>
                    <DataGridTextColumn Header="Última Manutenção" Binding="{Binding ÚltimaManutenção}" FontSize="17"/>
                    <DataGridTextColumn Header="Último Motorista" Binding="{Binding ÚltimoMotorista}" FontSize="17"/>
                </DataGrid.Columns>
                <!--<DataGrid.InputBindings>
                    <MouseBinding MouseAction="RightClick" Command=""></MouseBinding>
                </DataGrid.InputBindings>-->
                <!--<DataGrid.CellStyle>
                    <Style TargetType="{x:Type DataGridCell}">
                        <Setter Property="Template"></Setter>
                    </Style>
                </DataGrid.CellStyle>-->
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
