<UserControl x:Class="ControleFrota.Views.DialogWindows.WorkInProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Titulo}" Margin="20,5" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
        <TextBlock Text="{Binding PrimeiraLinha}" Grid.Row="1" FontSize="18" Margin="20,5" HorizontalAlignment="Center" MaxWidth="333" TextWrapping="Wrap"/>
        <ProgressBar IsIndeterminate="True" Margin="15" Grid.Row="2" Height="28"/>
    </Grid>

</UserControl>
